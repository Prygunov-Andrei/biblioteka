# 🔌 API Documentation

## Обзор

API проекта Biblioteka построен на Django REST Framework и предоставляет полный набор endpoints для управления библиотекой книг.

## Базовый URL

```
http://localhost:8000/api/
```

## Аутентификация

На данный момент API доступен без аутентификации. В будущем планируется добавить JWT токены.

## Формат ответов

Все ответы возвращаются в формате JSON.

### Успешные ответы
- `200 OK` — успешный запрос
- `201 Created` — объект создан
- `204 No Content` — объект удален

### Ошибки
- `400 Bad Request` — ошибка валидации
- `404 Not Found` — объект не найден
- `500 Internal Server Error` — ошибка сервера

## Основные endpoints

### 1. Categories (Категории)
- `GET /api/categories/` — список категорий
- `POST /api/categories/` — создание категории
- `GET /api/categories/{slug}/` — детали категории
- `PUT/PATCH /api/categories/{slug}/` — обновление
- `DELETE /api/categories/{slug}/` — удаление

### 2. Authors (Авторы)
- `GET /api/authors/` — список авторов
- `POST /api/authors/` — создание автора
- `GET /api/authors/{id}/` — детали автора
- `GET /api/authors/{id}/books/` — книги автора
- `PUT/PATCH /api/authors/{id}/` — обновление
- `DELETE /api/authors/{id}/` — удаление

### 3. Publishers (Издательства)
- `GET /api/publishers/` — список издательств
- `POST /api/publishers/` — создание издательства
- `GET /api/publishers/{id}/` — детали издательства
- `GET /api/publishers/{id}/books/` — книги издательства
- `PUT/PATCH /api/publishers/{id}/` — обновление
- `DELETE /api/publishers/{id}/` — удаление

### 4. Books (Книги)
- `GET /api/books/` — список книг (с фильтрами)
- `POST /api/books/` — создание книги
- `GET /api/books/{id}/` — детали книги
- `PUT/PATCH /api/books/{id}/` — обновление
- `DELETE /api/books/{id}/` — удаление

**Кастомные actions:**
- `GET /api/books/{id}/images/` — список изображений
- `POST /api/books/{id}/images/` — загрузка изображения
- `GET /api/books/{id}/electronic_versions/` — список электронных версий
- `POST /api/books/{id}/electronic_versions/` — добавление электронной версии
- `GET /api/books/{id}/pages/` — список страниц
- `POST /api/books/{id}/upload_pages/` — загрузка страниц
- `POST /api/books/{id}/process_pages/` — обработка страниц

### 5. Book Images (Изображения книг)
- `GET /api/book-images/` — список изображений
- `POST /api/book-images/` — создание изображения
- `GET /api/book-images/{id}/` — детали изображения
- `PUT/PATCH /api/book-images/{id}/` — обновление
- `DELETE /api/book-images/{id}/` — удаление

### 6. Book Electronic (Электронные версии)
- `GET /api/book-electronic/` — список электронных версий
- `POST /api/book-electronic/` — создание электронной версии
- `GET /api/book-electronic/{id}/` — детали
- `PUT/PATCH /api/book-electronic/{id}/` — обновление
- `DELETE /api/book-electronic/{id}/` — удаление

### 7. Book Pages (Страницы книг)
- `GET /api/book-pages/` — список страниц
- `POST /api/book-pages/` — создание страницы
- `GET /api/book-pages/{id}/` — детали страницы
- `POST /api/book-pages/{id}/process/` — обработка страницы
- `PUT/PATCH /api/book-pages/{id}/` — обновление
- `DELETE /api/book-pages/{id}/` — удаление

## Фильтрация и поиск

### Books
- `category` — фильтр по категории (slug или ID)
- `author` — фильтр по автору (ID или имя)
- `publisher` — фильтр по издательству (ID или название)
- `year_min`, `year_max` — диапазон годов издания
- `binding_type` — тип переплета
- `format` — формат книги
- `condition` — состояние
- `price_min`, `price_max` — диапазон цены
- `search` — поиск по названию, подзаголовку, ISBN, автору
- `ordering` — сортировка

### Authors
- `search` — поиск по ФИО
- `birth_year_min`, `birth_year_max` — диапазон годов рождения
- `ordering` — сортировка

### Publishers
- `search` — поиск по названию или городу
- `city` — фильтр по городу
- `ordering` — сортировка

## Подробная документация

Полная документация всех endpoints с примерами: [endpoints.md](endpoints.md)

---

**Последнее обновление:** 2025-11-03

