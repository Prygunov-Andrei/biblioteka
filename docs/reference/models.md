# 📊 Models Reference

## Обзор моделей

## Система пользователей

### UserProfile (Профиль пользователя)
Профиль пользователя, создается автоматически при создании пользователя.

**Поля:**
- `user` (OneToOne → AUTH_USER_MODEL) - Связь с Django User (использует settings.AUTH_USER_MODEL)
- `full_name` (CharField) - Полное имя (ФИО)
- `photo` (ImageField) - Фото профиля
- `description` (TextField) - Описание/биография
- `created_at`, `updated_at` - Временные метки

**Особенности:**
- Автоматически создается при создании пользователя (сигнал)
- Доступен через `user.profile`

### Library (Библиотека)
Физические библиотеки пользователей. У пользователя может быть несколько библиотек.

**Поля:**
- `owner` (ForeignKey → AUTH_USER_MODEL) - Владелец библиотеки (использует settings.AUTH_USER_MODEL)
- `name` (CharField) - Название библиотеки
- `address` (TextField) - Физический адрес
- `city` (CharField) - Город
- `country` (CharField) - Страна
- `description` (TextField) - Описание библиотеки
- `created_at`, `updated_at` - Временные метки

**Связи:**
- `books` - книги в библиотеке (ForeignKey из Book)

**Ограничения:**
- Пользователь может создавать/удалять только свои библиотеки
- При удалении библиотеки книги остаются у владельца, но `library` становится null
- При удалении пользователя библиотеки удаляются (CASCADE)

### Hashtag (Хэштег)
Хэштеги для книг. Могут быть общими или персональными.

**Поля:**
- `name` (CharField) - Название хэштега (например, "#фантастика")
- `slug` (SlugField) - URL-дружественное имя (уникальный)
- `creator` (ForeignKey → AUTH_USER_MODEL, null=True) - Создатель хэштега (null = общий, использует settings.AUTH_USER_MODEL)
- `created_at` - Дата создания

**Связи:**
- `books` - книги с этим хэштегом (ManyToMany через BookHashtag)

**Особенности:**
- Пользователи могут создавать свои хэштеги
- Могут быть общие хэштеги (creator=null)
- Автоматическое создание slug из названия
- Уникальность (name, creator)

### BookHashtag (Связь книги и хэштега)
Промежуточная модель для связи Book ↔ Hashtag.

**Поля:**
- `book` (ForeignKey → Book) - Книга
- `hashtag` (ForeignKey → Hashtag) - Хэштег
- `created_at` - Дата добавления

**Ограничения:**
- Максимум 20 хэштегов на книгу (валидация)
- Уникальность (book, hashtag)

### BookReview (Отзыв на книгу)
Персональные отзывы пользователей на книги.

**Поля:**
- `book` (ForeignKey → Book) - Книга
- `user` (ForeignKey → AUTH_USER_MODEL) - Автор отзыва (использует settings.AUTH_USER_MODEL)
- `rating` (IntegerField, 1-5) - Оценка (опционально)
- `review_text` (TextField) - Текст отзыва
- `created_at`, `updated_at` - Временные метки

**Ограничения:**
- Уникальность (book, user) - один отзыв на книгу от пользователя
- Пользователь может редактировать только свои отзывы
- При удалении пользователя отзывы удаляются (CASCADE)

---

## Основные модели

### Category (Категория)
Категория книг с кодом для классификации.

**Поля:**
- `code` (CharField) - Буквенный код категории (уникальный)
- `name` (CharField) - Название категории
- `slug` (SlugField) - URL-дружественное имя (уникальный)
- `icon` (CharField) - Иконка (эмодзи)
- `order` (IntegerField) - Порядок сортировки
- `created_at` (DateTimeField) - Дата создания

**Связи:**
- `books` - все книги категории (ForeignKey)

### Author (Автор)
Автор книги.

**Поля:**
- `full_name` (CharField) - ФИО автора
- `birth_year` (IntegerField) - Год рождения
- `death_year` (IntegerField) - Год смерти
- `biography` (TextField) - Биография
- `notes` (TextField) - Заметки
- `created_at`, `updated_at` - Временные метки

**Связи:**
- `books` - все книги автора (ManyToMany через BookAuthor)

### Publisher (Издательство)
Издательство.

**Поля:**
- `name` (CharField) - Название
- `city` (CharField) - Город
- `website` (URLField) - Ссылка на сайт
- `description` (TextField) - Описание
- `created_at`, `updated_at` - Временные метки

**Связи:**
- `books` - все книги издательства (ForeignKey)

### Book (Книга)
Основная модель книги.

**Поля (владелец и библиотека):**
- `owner` (ForeignKey → AUTH_USER_MODEL, CASCADE) - Владелец книги (создатель, использует settings.AUTH_USER_MODEL)
- `library` (ForeignKey → Library, SET_NULL) - Библиотека, в которой находится книга
- `status` (CharField, choices) - Статус книги

**Статусы книги:**
- `none` - Без статуса
- `read` - Прочитано
- `want_to_read` - Буду читать
- `want_to_reread` - Буду перечитывать

**Поля (основные):**
- `category` (ForeignKey) - Категория
- `authors` (ManyToMany) - Авторы (до 3-х)
- `hashtags` (ManyToMany через BookHashtag) - Хэштеги (до 20)
- `title` (CharField) - Название
- `subtitle` (TextField) - Второе название
- `publication_place` (CharField) - Место издания
- `publisher` (ForeignKey) - Издательство
- `year` (IntegerField) - Год издания (точный)
- `year_approx` (CharField) - Год приблизительно (197?, 18??)
- `pages_info` (CharField) - Информация о страницах
- `binding_type` (CharField, choices) - Тип переплета
- `binding_details` (CharField) - Детали переплета
- `format` (CharField, choices) - Формат книги
- `price_rub` (DecimalField) - Цена в рублях
- `description` (TextField) - Содержание/аннотация
- `condition` (CharField, choices) - Состояние
- `condition_details` (TextField) - Детали состояния
- `seller_code` (CharField) - Учетный код продавца
- `isbn` (CharField) - ISBN
- `created_at` - Дата размещения
- `updated_at` - Дата обновления

**Properties:**
- `images_count` - Количество изображений

**Связи:**
- `images` - изображения книги (BookImage)
- `electronic_versions` - электронные версии (BookElectronic)
- `pages_set` - страницы книги (BookPage)
- `reviews` - отзывы на книгу (BookReview)
- `book_hashtags` - связи с хэштегами (BookHashtag)
- `owned_books` - книги пользователя (related_name)

**Особенности:**
- `owner` автоматически устанавливается при создании (создатель)
- `owner` всегда остается первоначальным создателем (не меняется при передаче)
- `library` показывает текущее местоположение книги
- При удалении пользователя книги удаляются (CASCADE)

### BookAuthor (Связь книги и автора)
Промежуточная модель для связи Book ↔ Author с порядком.

**Поля:**
- `book` (ForeignKey) - Книга
- `author` (ForeignKey) - Автор
- `order` (IntegerField) - Порядок (1-3)

**Ограничения:**
- Максимум 3 автора на книгу
- Уникальность (book, order)

### BookImage (Изображение книги)
Изображение книги (до 20 шт.).

**Поля:**
- `book` (ForeignKey) - Книга
- `image` (ImageField) - Изображение
- `order` (IntegerField) - Порядок (1-20)
- `created_at` - Дата создания

**Ограничения:**
- Максимум 20 изображений на книгу
- Уникальность (book, order)

### BookElectronic (Электронная версия)
Электронная версия книги.

**Поля:**
- `book` (ForeignKey) - Книга
- `format` (CharField, choices) - Формат (PDF, EPUB, MOBI, FB2, DJVU, TXT, RTF, DOC, DOCX)
- `url` (URLField) - Ссылка на файл
- `file` (FileField) - Загруженный файл
- `created_at` - Дата создания

### BookPage (Страница книги)
Страница книги для обработки документов.

**Поля:**
- `book` (ForeignKey) - Книга
- `page_number` (IntegerField) - Номер страницы
- `original_image` (ImageField) - Оригинальное изображение
- `processed_image` (ImageField) - Обработанное изображение
- `processing_status` (CharField, choices) - Статус обработки
- `processed_at` (DateTimeField) - Дата обработки
- `error_message` (TextField) - Сообщение об ошибке
- `width`, `height` (IntegerField) - Размеры изображения
- `created_at` - Дата создания

**Статусы:**
- `pending` - Ожидает
- `processing` - Обрабатывается
- `completed` - Готово
- `failed` - Ошибка

---

## Примечания

### Использование AUTH_USER_MODEL

Все модели используют `settings.AUTH_USER_MODEL` вместо прямого импорта `User`. Это позволяет:
- Использовать кастомную модель пользователя
- Легче тестировать
- Следовать best practices Django

В коде используется `get_user_model()` для получения модели пользователя.

---

**Последнее обновление:** 2025-11-03 (после рефакторинга)

