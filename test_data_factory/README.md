# 🏭 Фабрика тестовых данных

Фабрика для генерации тестовых книг с использованием реальных авторов и издательств.

## 📁 Структура

```
test_data_factory/
├── README.md                          # Этот файл
├── PLAN.md                            # План реализации
├── factory.py                         # Главная фабрика
├── data/                              # JSON данные
│   ├── russian_publishers_unified.json
│   └── russian_authors_batch1_structured.json
├── generators/                        # Генераторы
│   ├── publishers_loader.py           # Загрузчик издательств
│   ├── authors_loader.py              # Загрузчик авторов
│   ├── titles_generator.py            # Генератор названий
│   ├── image_generator.py             # Генератор изображений
│   └── book_generator.py              # Генератор данных книг
└── generated_images/                  # Временные изображения (в .gitignore)
```

## 🚀 Использование

### 1. Загрузка авторов и издательств (для деплоя)

Загружает всех авторов и издательства из JSON в базу данных. Используется при первом деплое на чистую БД.

```bash
python manage.py load_authors_and_publishers
```

### 2. Генерация тестовых книг

Генерирует тестовые книги для всех категорий.

```bash
# По умолчанию: 3 книги на категорию
python manage.py generate_test_books

# Указать количество книг на категорию
python manage.py generate_test_books --count-per-category 5

# Указать пользователя и библиотеку
python manage.py generate_test_books --user-id 1 --library-id 2
```

## 📊 Что генерируется

### Книги
- Для каждой категории создается указанное количество книг
- Все поля заполняются реалистичными данными
- Все авторы из JSON используются хотя бы один раз
- Все издательства из JSON используются хотя бы один раз

### Изображения
- Для каждой книги создается 3 изображения
- Главное изображение (order=1): однотонный фон + название книги крупным шрифтом
- Остальные (order=2, 3): однотонные цветные фоны
- Размер: 800x1200 пикселей (портретная ориентация)

### Авторы и издательства
- Автоматически загружаются в БД при первом запуске
- Проверяется существование (по `full_name` для авторов, по `name` для издательств)
- Существующие записи не дублируются

## 🎯 Особенности

1. **Распределение ресурсов**: Гарантируется использование всех авторов и издательств хотя бы один раз
2. **Реалистичные данные**: Названия книг, описания и метаданные генерируются реалистично
3. **Автоматическая очистка**: Временные изображения удаляются после генерации
4. **Транзакции**: Все операции выполняются в транзакциях для целостности данных

## 📝 Зависимости

- `Pillow` - для генерации изображений
- Django модели из `books.models`

## ⚠️ Важно

- Фабрика создает тестовые данные - после тестирования книги нужно удалить
- Авторы и издательства остаются в БД (это справочные данные)
- Временные изображения сохраняются в `generated_images/` и автоматически удаляются

## 🔧 Структура данных

### Авторы (russian_authors_batch1_structured.json)
```json
{
  "Фамилия": "Пушкин",
  "Имя": "Александр",
  "Отчество": "Сергеевич",
  "год рождения": 1799,
  "год смерти": 1837,
  "короткая биография": "..."
}
```

### Издательства (russian_publishers_unified.json)
```json
{
  "Название": "АСТ",
  "Город": "Москва",
  "ссылка на сайт": "https://ast.ru/",
  "Описание": "..."
}
```

---

**Последнее обновление:** 2025-01-03

